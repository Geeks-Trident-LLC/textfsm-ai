# SDK for the vertexai provider extra, via anyask.
#
# Mirrors the `vertexai` extra in pyproject.toml - keep in sync if that
# changes. See https://github.com/Geeks-Trident-LLC/anyask for the SDK
# version pin anyask[vertexai] resolves to.
anyask[vertexai]>=0.1.1
