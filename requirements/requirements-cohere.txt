# SDK for the cohere provider extra, via anyask.
#
# Mirrors the `cohere` extra in pyproject.toml - keep in sync if that
# changes. See https://github.com/Geeks-Trident-LLC/anyask for the SDK
# version pin anyask[cohere] resolves to.
anyask[cohere]>=0.1.1
