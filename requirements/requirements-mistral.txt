# SDK for the mistral provider extra, via anyask.
#
# Mirrors the `mistral` extra in pyproject.toml - keep in sync if that
# changes. See https://github.com/Geeks-Trident-LLC/anyask for the SDK
# version pin anyask[mistral] resolves to.
anyask[mistral]>=0.1.1
