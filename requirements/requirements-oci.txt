# SDK for the oci provider extra, via anyask.
#
# Mirrors the `oci` extra in pyproject.toml - keep in sync if that
# changes. See https://github.com/Geeks-Trident-LLC/anyask for the SDK
# version pin anyask[oci] resolves to.
anyask[oci]>=0.1.1
