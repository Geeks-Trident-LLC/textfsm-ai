# SDK for the gemini provider extra, via anyask.
#
# Mirrors the `gemini` extra in pyproject.toml - keep in sync if that
# changes. See https://github.com/Geeks-Trident-LLC/anyask for the SDK
# version pin anyask[gemini] resolves to.
anyask[gemini]>=0.1.1
