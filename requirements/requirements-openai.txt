# SDK for the openai provider extra, via anyask.
#
# Mirrors the `openai` extra in pyproject.toml - keep in sync if that
# changes. See https://github.com/Geeks-Trident-LLC/anyask for the SDK
# version pin anyask[openai] resolves to.
anyask[openai]>=0.1.1
