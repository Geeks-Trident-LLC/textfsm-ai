# SDK for the bedrock provider extra.
# Pinned exactly: 1.42.97 is the newest boto3 release with a
# Python 3.9-compatible botocore; verified via
# `pip download --python-version 3.9` resolution, since boto3 bumps
# its Python floor frequently across even small version bumps.
#
# Mirrors the `bedrock` extra in pyproject.toml - keep in sync if that
# changes.
boto3==1.42.97
