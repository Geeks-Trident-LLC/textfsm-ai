# SDK for the bedrock provider extra, via anyask.
#
# Mirrors the `bedrock` extra in pyproject.toml - keep in sync if that
# changes. See https://github.com/Geeks-Trident-LLC/anyask for the SDK
# version pin anyask[bedrock] resolves to.
anyask[bedrock]>=0.1.1
